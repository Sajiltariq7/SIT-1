# [Your Chosen Free Tool — e.g. Aider / Roo Code / Continue]

## 1. What is it?
(CLI, IDE/extension, desktop app, or a combination? One or two sentences.)

## 2. Is it free? What did you actually use?
(State clearly: is the tool itself free/open source? Does normal use need a paid
model/API key? Exactly which model/provider did YOU use, at no cost?)

## 3. Setup — reproducible commands
```bash
# paste the exact commands you ran, in order
```
Prerequisites: (Node version, Python version, API key needed, etc.)

## 4. Codebase understanding
(Prompt you used to ask it to summarize demo-codebase/. Paste its response,
or a shortened version. Then: did it correctly identify the entry point and
data flow? Did it invent anything that wasn't true? How did it seem to gather
context — did it read every file, use search, build an index, etc.?)

## 5. Project instructions, skills, plugins, MCP support
- Did it read and follow `AGENTS.md`? (yes/no + evidence)
- Skills: supported? tested or not tested?
- Plugins/extensions: supported? tested or not tested?
- MCP: supported? tested or not tested?
(Clearly separate "unsupported" from "supported but I didn't test it".)

## 6. Practical task performed
- **Change task:** prompt used, diff produced, files changed, evidence path
- **Debug task:** prompt used, root cause it found, fix applied, evidence path
- **Test verification:** command run, output before/after, evidence path

## 7. Permissions & approval workflow
(What did it ask permission for? Could you see the command/diff BEFORE it
ran? Could you reject it? Screenshot reference.)

## 8. What worked well / what failed / what you had to fix manually
(Be honest — this is worth real points. A tool with limitations you clearly
explain is better than a tool you claim was perfect.)

## 9. Best use cases, limitations, and recommendation
(Would you recommend this to the team? For what kind of work specifically?)
